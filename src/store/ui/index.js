import reducer from './reducer';
import * as uiAction from './action';
import * as uiOperation from './operation';
import * as uiTypes from './types';

export {
  uiAction,
  uiOperation,
  uiTypes,
};

export default reducer;
